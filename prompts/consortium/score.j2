Score the following hypotheses for a cold-case review.

Calibration:
- 0.0–0.1: Effectively ruled out by the evidence.
- 0.1–0.3: Possible but poorly supported.
- 0.3–0.5: Plausible; significant gaps remain.
- 0.5–0.7: Well-supported; preferred among alternatives.
- 0.7–0.9: Strongly supported; would require new evidence to overturn.
- 0.9–1.0: Essentially the only viable explanation.

Most reasonable hypotheses should fall in 0.2–0.7. Scores above 0.7 should
be rare and justified by specific evidence.

================ CASE NARRATIVE ================

{{ case.narrative }}

================ EVIDENCE ================

{% for item in evidence %}
[{{ item.id }}] {{ item.title }} ({{ item.reliability }})
{{ item.content }}

{% endfor %}

================ HYPOTHESES TO SCORE ================

{% for h in hypotheses %}
[{{ h.id }}] {{ h.one_line_summary }}

{{ h.narrative }}

Evidence support claimed:
{% for es in h.evidence_support %}
- {{ es.evidence_id }}: {{ es.role }} (weight {{ es.weight }})
{% endfor %}

{% endfor %}

================ TASK ================

For each hypothesis, produce a HypothesisScore with:
- hypothesis_id
- score (float in [0.0, 1.0])
- rationale (one sentence)

Output: { "scores": [<HypothesisScore>, ...] }